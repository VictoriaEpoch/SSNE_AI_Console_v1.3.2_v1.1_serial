# 串口命令接口文档

本文整理当前上位机会通过串口与板端交互的命令，以及上位机当前会解析的主要回包格式。命令已按板端 `UART_COMMANDS.md` 核对，主要发送入口位于 `app/main_window.py`，危险区和人脸录入命令构造位于 `app/commands.py`。

## 1. 通信约定

- 串口参数：`115200 8-N-1`。
- 命令格式：ASCII 文本命令，按行发送。
- 行结束符：上位机发送时自动追加 `CRLF`，即 `\r\n`。
- 长度限制：单条命令内容最多 `24` 个字符，不计算结尾 `CRLF`；为避免中文等多字节字符绕过板端缓冲限制，UTF-8 编码后也不得超过 `24` 字节。
- 超限处理：自动命令、日志终端手动命令和底层发送队列使用同一校验；超限命令会在发送前被完整拦截并报告，绝不会静默截断。
- 命令名：板端不区分大小写，本文统一使用小写写法。
- 布尔参数：板端支持 `on/off`、`1/0`、`true/false`、`yes/no`，界面按钮统一发送 `on/off`。
- 手动命令：日志终端可直接输入任意板端命令；上位机只负责按行发送和显示回包。
- 自动轮询：连接成功后，上位机默认每秒发送一次 `status`；启用智能家居手势控制时，`status` 加快到约 250ms 一次。启用久坐监测，或当前时间处于已启用的起夜监测时段时，还会每秒发送一次 `debug`。

## 2. 上位机自动发送命令

| 命令 | 触发时机 | 用途 | 备注 |
| --- | --- | --- | --- |
| `status` | 连接成功后立即发送一次；普通状态约每秒一次；智能家居启用时约 250ms 一次 | 读取板端紧凑状态 | 智能家居手势、跌倒、危险区、FPS 等页面状态主要来自该命令回包 |
| `debug` | 连接成功后在需要姿态监测时发送，之后按需每秒发送 | 读取姿态调试字段 | 久坐和起夜告警依赖 `sv/sc` 姿态字段 |

板端还支持 `debug on` 和 `debug off` 开关周期调试输出。当前上位机为了不改变板端周期输出开关，只自动发送单次 `debug`；如需周期调试输出，可在日志终端手动发送 `debug on` 或 `debug off`。

## 3. 推理模式命令

| 命令 | 页面入口 | 用途 |
| --- | --- | --- |
| `base` | 设备控制 > 推理模式 | 切换到基础检测模式 |
| `hand` | 设备控制或智能家居页 | 切换到手势模式 |
| `face` | 设备控制 > 推理模式 | 切换到人脸模式 |
| `pose` | 设备控制 > 推理模式 | 切换到姿态模式 |
| `all` | 设备控制、姿态控制或智能家居页 | 切换到全功能模式 |

板端返回 `[SERIAL] osd_mode set to ...` 或 `[SERIAL] inference_mode set to ...` 时，上位机会识别为模式确认回包，并更新当前模式显示。

板端兼容写法为：

```text
mode <base|hand|face|pose|all>
```

当前界面按钮发送的是直接模式名，例如 `pose`、`all`。

## 4. 显示与识别辅助命令

| 命令 | 页面入口 | 用途 |
| --- | --- | --- |
| `bg on` | 设备控制 > 背景开 | 开启静态背景显示 |
| `bg off` | 设备控制 > 背景关 | 关闭静态背景显示 |
| `kp on` | 设备控制 > 关键点开 | 开启姿态关键点显示 |
| `kp off` | 设备控制 > 关键点关 | 关闭姿态关键点显示 |
| `scan` | 设备控制 > 立即扫脸 | 立即触发一次人脸扫描 |

板端说明中 `bg`、`debug/db`、`kp` 不带参数时会切换对应开关。上位机界面按钮使用显式 `on/off`，避免当前状态不确定。

## 5. 人脸录入命令

### 5.1 提交录入

```text
reg <person_id> <display_name> [frames]
```

参数说明：

| 参数 | 约束 | 说明 |
| --- | --- | --- |
| `person_id` | 非空，不能包含空白字符 | 人员 ID |
| `display_name` | 非空，不能包含空白字符 | 显示名称 |
| `frames` | 可选，`1`～`120` 的整数 | 采集帧数，省略时板端默认 `15`；当前上位机界面会显式发送该参数 |

示例：

```text
reg u1 alice 20
reg u2 bob
```

板端返回 `[SERIAL] enroll request queued: ...` 时，上位机会显示录入排队；返回 `[SERIAL] enroll success: ...` 时，上位机会识别为录入成功。

### 5.2 取消录入

```text
reg cancel
```

用于取消当前待执行的人脸录入。板端返回包含 `enroll request canceled` 或 `no pending enroll` 的文本时，上位机会识别为录入取消或无待取消任务。

## 6. 状态打印周期命令

```text
set print_interval <value>
```

参数说明：

| 参数 | 约束 | 说明 |
| --- | --- | --- |
| `value` | 大于 `0` 的整数 | 板端状态打印周期 |

示例：

```text
set print_interval 120
```

板端返回 `[SERIAL] print_interval set to ...` 时，上位机会更新 `cfg_print_interval` 显示。

## 7. 性能测试命令

### 7.1 启动测试

```text
test [base|face|pose|all|each] [sec] [sensor_fps]
```

参数说明：

| 参数 | 约束 | 说明 |
| --- | --- | --- |
| `mode` | 可选，`base`、`face`、`pose`、`all`、`each` | 测试模式；当前上位机界面会显式发送该参数 |
| `seconds` / `sec` | 可选，大于 `0` 的整数 | 测试时长，单位秒；板端默认 `60` |
| `sensor_fps` | 可选，大于 `0` 的数字 | 传感器 FPS；板端默认 `30` |

示例：

```text
test all 30 15
test each 60 15
test 60 30
```

其中 `test 60 30` 是板端支持的简写，等价于 `test each 60 30`。当前上位机界面会发送完整形式，例如 `test all 30 15`。

### 7.2 停止测试

```text
test stop
```

用于停止当前性能测试。

## 8. 危险区域命令

危险区坐标使用归一化坐标，范围为 `0.0`～`1.0`。上位机从截图编辑器得到多边形顶点后，会按 `x1 y1 x2 y2 ...` 顺序发送。

### 8.1 设置危险区

```text
zone set <x1> <y1> ... <xn> <yn>
```

参数说明：

| 参数 | 约束 | 说明 |
| --- | --- | --- |
| `n` | `3`～`12` | 多边形顶点数量 |
| `x/y` | `0.0`～`1.0` | 相对完整摄像头画面的归一化坐标 |

示例：

```text
zone set 0 0 1 0 1 1
```

`zone set` 只有在整行不超过 24 字符时才能通过上位机日志终端发送。一般小数多边形会超限，因此界面统一采用下面的短命令逐点流程。

从界面点击一次“下发并启用”后，上位机会自动拆成短命令逐条发送：

```text
zone clear
zone add <x1> <y1>
zone add <x2> <y2>
...
zone add <xn> <yn>
zone on
zone list
```

用户不需要逐条操作。上位机发送一条后会等待板端对应的成功回包，确认后再发送下一条；下发期间暂停自动 `status/debug` 轮询，并在危险区域页面实时显示正在清空、当前确认点数、正在启用、最终成功或具体超时步骤。任一步收到 `[SERIAL][ERROR]` 或等待确认超过 2 秒时会停止后续发送，防止在设备状态不明确时继续写入。

危险区逐点命令使用四位小数，例如 `zone add 0.2000 0.8000`，长度为 22 字符。日志终端仍可手动使用能放进 24 字符的紧凑 `zone set`，但界面按钮采用上述逐点确认方式，以规避长命令和连续命令拥挤问题。

### 8.2 启停与查询

| 命令 | 页面入口 | 用途 |
| --- | --- | --- |
| `zone` | 日志终端手动发送 | 显示当前危险区域配置 |
| `zone on` | 姿态控制 > 危险区域 > 启用 | 启用板端危险区判断 |
| `zone off` | 姿态控制 > 危险区域 > 停用 | 停用板端危险区判断 |
| `zone list` | 姿态控制 > 危险区域 > 查询 | 查询板端危险区状态 |
| `zone clear` | 姿态控制 > 危险区域 > 清空 | 清空设备端和本地保存的危险区 |
| `zone pop` | 日志终端手动发送 | 删除最后一个区域点 |
| `zone add <x> <y>` | 日志终端手动发送 | 追加一个区域点 |
| `zone rect <x1> <y1> <x2> <y2>` | 姿态控制 > 危险区域 > 测试矩形，或日志终端手动发送 | 使用两个角点设置矩形区域 |

“测试矩形”按钮也会使用与普通多边形相同的逐点确认流程，一键自动发送：

```text
zone clear
zone add 0.2000 0.2000
zone add 0.8000 0.2000
zone add 0.8000 0.8000
zone add 0.2000 0.8000
zone on
zone list
```

`zone rect`、`zone set` 和 `zone pop` 仍是板端支持的手动调试写法，界面没有单独输入步骤。

## 9. 调试帮助命令

| 命令 | 页面入口 | 用途 |
| --- | --- | --- |
| `help` | 日志终端 > 发送 help | 查看板端帮助 |
| `pro` | 日志终端 > 发送 pro | 查看板端专业/扩展帮助 |

日志终端也允许输入本文未列出的板端命令。若后续板端新增命令，而上位机暂未做专门按钮，也可以先通过日志终端验证。

## 10. 快捷调参与完整参数命令

以下命令由板端支持，当前上位机没有专门 UI 控件；需要时可在日志终端手动发送。参数修改仅作用于当前运行实例，板端程序重启后恢复编译时默认值。

日志终端同样执行 24 字符硬限制。优先使用下表的短别名；第 10.2 节中部分完整 `set ...` 命令名称本身已经超过 24 字符，会被当前上位机拦截。如确需从此串口链路调整这些参数，需要板端补充不超过 24 字符的短别名。

### 10.1 快捷调参

紧凑形式和空格形式均可使用，例如 `pa0.70` 与 `pa 0.70` 等价。

| 快捷命令 | 完整命令 | 参数范围 |
| --- | --- | --- |
| `pa <value>` | `set pose_ema_alpha <value>` | `0~1` |
| `pp <value>` | `set pose_pending_iou <value>` | `0~1` |
| `ps <value>` | `set pose_switch_frames <value>` | `1~120` |
| `ph <value>` | `set pose_hold_frames <value>` | `1~120` |
| `fr <value>` | `set fall_recover_frames <value>` | `0~180` |
| `fd <value>` | `set fall_decay_unreliable <value>` | `0~1` |
| `fi <value>` | `set face_interval <value>` | 正整数 |
| `pi <value>` | `set print_interval <value>` | 正整数 |
| `di <value>` | `set debug_interval <value>` | 正整数 |

### 10.2 完整参数命令

| 命令 | 范围或取值 |
| --- | --- |
| `set print_interval <N>` | 正整数，状态输出间隔帧数 |
| `set debug_interval <N>` | 正整数，调试输出间隔帧数 |
| `set face_interval <N>` | 正整数，人脸更新间隔帧数 |
| `set pose_kpt_conf <value>` | `0~1` |
| `set pose_ema_alpha <value>` | `0~1` |
| `set pose_track_iou <value>` | `0~1` |
| `set pose_switch_margin <value>` | `0~1` |
| `set pose_pending_iou <value>` | `0~1` |
| `set pose_switch_frames <N>` | `1~120` |
| `set pose_hold_frames <N>` | `1~120` |
| `set pose_kpt_draw <on|off>` | 开关 |
| `set pose_lstm_input <raw|smooth>` | 原始或平滑关键点 |
| `set fall_process_enter <value>` | `0~1` |
| `set fall_process_confirm <value>` | `0~1` |
| `set fall_process_exit <value>` | `0~1` |
| `set fall_result_enter <value>` | `0~1` |
| `set fall_result_exit <value>` | `0~1` |
| `set fall_lstm_pre <value>` | `0~1` |
| `set fall_lstm_enter <value>` | `0~1` |
| `set fall_lstm_exit <value>` | `0~1` |
| `set fall_lstm_strong <value>` | `0~1` |
| `set fall_score_alpha <value>` | `0~1` |
| `set fall_decay_unreliable <value>` | `0~1` |
| `set fall_tilt_process_deg <value>` | `1~89` |
| `set fall_tilt_result_deg <value>` | `1~89` |
| `set fall_result_aspect <value>` | `0.1~3` |
| `set fall_drop_speed <value>` | `0~1` |
| `set fall_hard_center <value>` | `0~1` |
| `set fall_hard_shrink <value>` | `0~1` |
| `set fall_hard_tilt <value>` | `1~89` |
| `set fall_hard_drop <value>` | `0~1` |
| `set fall_pre_frames <N>` | `1~120` |
| `set fall_confirm_frames <N>` | `1~120` |
| `set fall_recover_frames <N>` | `0~180` |

## 11. 兼容别名

以下写法由板端兼容，日志终端可手动发送；上位机界面按钮统一使用推荐写法。

| 兼容写法 | 推荐写法 |
| --- | --- |
| `h` / `?` | `help` |
| `professional` | `pro` |
| `st` | `status` |
| `db on/off` | `debug on/off` |
| `pk on/off` | `kp on/off` |
| `osd0` / `bg0` | `bg` |
| `det` | `scan` |
| `enroll <id> <name> [frames]` | `reg <id> <name> [frames]` |
| `cancel_enroll` | `reg cancel` |
| `bench` / `perf` | `test` |
| `dz` | `zone` |

旧版 `bench all` / `perf all` 表示依次测试多个模式，而标准命令 `test all` 只测试完整 `all` 链路。新脚本和现场操作应统一使用 `test`。

## 12. 上位机当前解析的回包

### 12.1 状态回包

上位机兼容两套状态格式：

```text
[SERIAL][S] f=... fs=... p=...
[SERIAL][STATUS] frame=... fall_status=... person_active=...
```

常用字段：

| 紧凑字段 | 长字段 | 说明 |
| --- | --- | --- |
| `f` | `frame` | 帧号 |
| `fs` | `fall_status` | 跌倒状态 |
| `fp` | `fall_prob` | 跌倒概率 |
| `p` | `person_active` | 是否检测到人员 |
| `b` | `base` | 基础检测状态 |
| `fc` | `face` | 人脸检测状态 |
| `hc` | `hand` | 手势检测状态 |
| `gc` | `gesture` | 手势类别，`0`～`4` 对应 GA～GE |
| `gs` | `gesture_score` | 手势分数 |
| `om` | `osd_mode` | 当前显示/推理模式 |
| `pi` | `cfg_print_interval` | 状态打印周期 |
| `fi` | `fps_inst` | 瞬时 FPS |
| `fa` | `fps_avg` | 平均 FPS |
| `dz` | - | 危险区是否开启 |
| `dzn` | - | 危险区顶点数量 |
| `dzt` | - | 人员目标数 |
| `dzh` | - | 人员命中数 |
| `dza` | - | 人员危险区告警 |
| `pzt` | - | 宠物目标数 |
| `pzh` | - | 宠物命中数 |
| `pza` | - | 宠物危险区告警 |

状态输出中的 `ep=1` 表示存在录入请求，`er=当前帧数/目标帧数` 表示录入进度。上位机会保留这些原始字段并显示在字段表中。

### 12.2 调试回包

```text
[SERIAL][D] f=... sv=... sc=...
```

常用字段：

| 紧凑字段 | 长字段 | 说明 |
| --- | --- | --- |
| `f` | `frame` | 帧号 |
| `sv` | `posture_valid` | 姿态是否有效 |
| `sc` | `posture_class` | 姿态类别，当前上位机按 `0=LYING`、`1=SITTING`、`2=STANDING` 使用 |
| `sg` | - | 第一名与第二名决策分数间隔 |
| `ss` | - | `LYING/SITTING/STANDING` 三类决策分数 |
| `sgo` | - | 是否由坐姿几何将 `STANDING` 修正为 `SITTING` |
| `sgl` / `sgs` | - | 有效腿数量与坐姿几何分数 |
| `str` / `ska` | - | 大腿小腿比例与膝关节角度 |

久坐告警使用 `sv=1` 且 `sc=1` 的连续时长；起夜告警先用 `sc=0` 确认躺卧，再用 `sc=1` 或 `sc=2` 确认起身。

受控坐下保护字段 `cscd/csf/cse/csg/csc/csx`、跌倒调试字段 `fp/fpr/fps`、`psc/rsc`、`tlt/asp/dsp`、`pf/cf/rf`、`pe/pc/re/lp/le/we` 会作为原始字段进入字段表；当前上位机没有为这些字段设置额外 UI 别名。

### 12.3 告警与确认回包

| 回包前缀或内容 | 上位机识别类型 | 用途 |
| --- | --- | --- |
| `[SERIAL][PET_DANGER][ALERT]` | `pet_danger_alert` | 宠物进入危险区告警 |
| `[SERIAL][PET_DANGER] ... clear ...` | `pet_danger_clear` | 宠物危险区告警解除 |
| `[SERIAL][ERROR]` | `error` | 错误信息 |
| `[SERIAL][WARN]` | `warning` | 警告信息 |
| `[SERIAL] enroll success:` | `enroll_success` | 人脸录入成功 |
| `[SERIAL] enroll request queued:` | `enroll_queued` | 人脸录入已排队 |
| 包含 `enroll request canceled` | `enroll_canceled` | 录入取消 |
| 包含 `no pending enroll` | `enroll_canceled` | 没有待取消录入 |
| `[SERIAL] osd_mode set to ...` | `mode_ack` | 模式切换确认 |
| `[SERIAL] inference_mode set to ...` | `mode_ack` | 模式切换确认 |
| `[SERIAL] print_interval set to ...` | `interval_ack` | 打印周期设置确认 |
| `[FACE_MATCH] ...` | `raw` | 人脸调试输出，当前显示为原始日志 |
| `[PERF] ...` | `raw` | 性能测试输出，当前显示为原始日志 |
| 其他 `[SERIAL]...` | `ack` | 普通确认或提示 |
| 其他文本 | `raw` | 原始日志 |

## 13. 当前不直接下发的功能

- 久坐告警、起夜告警和智能家居手势映射均在上位机本地实现，不会向板端发送专用配置命令。
- 智能家居模拟动作只改变上位机内存中的模拟设备状态，不会通过串口向真实家电发送指令。
- 截图和多边形编辑在上位机本地完成；只有归一化后的危险区顶点会通过 `zone clear`、多条 `zone add`、`zone on` 和 `zone list` 自动下发。
