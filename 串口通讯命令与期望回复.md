# 串口通讯命令与期望回复

本文按当前上位机代码整理实际会发送或允许手动发送的 UART 命令，以及板端期望返回的文本格式。命令发送入口主要在 `app/main_window.py`，命令构造和长度校验在 `app/commands.py`，回包解析在 `app/protocol.py`。

## 1. 通讯约束

| 项目 | 约定 |
| --- | --- |
| 串口参数 | `115200 8-N-1` |
| 命令格式 | ASCII 文本命令，按行发送 |
| 行结束符 | 上位机发送时追加 `CRLF`，即 `\r\n` |
| 长度限制 | 单条命令内容最长 `24` 个字符，且 UTF-8 编码后最长 `24` 字节；不计算结尾 `CRLF` |
| 超限处理 | 上位机发送入口和底层队列都会拦截超限命令，不截断、不继续发送 |
| 失败回包 | 任意命令若收到 `[SERIAL][ERROR] ...`，上位机会按错误处理并显示日志 |
| 警告回包 | `[SERIAL][WARN] ...` 会显示为警告日志 |

当前自动生成命令最长为：

```text
zone add 0.1234 0.9876
```

长度为 `22` 个字符，低于 24 字符限制。

## 2. 状态轮询命令

| 命令 | 触发入口 | 期望回复 | 上位机判定 |
| --- | --- | --- | --- |
| `status` | 连接成功后立即发送；普通轮询约 1 秒一次；智能家居启用后约 250ms 一次 | `[SERIAL][S] f=... fs=... p=...` 或 `[SERIAL][STATUS] frame=... fall_status=...` | 解析为 `status` 事件，刷新人员、人脸、手势、跌倒、危险区、FPS 等状态 |
| `debug` | 需要姿态监测时自动发送，约 1 秒一次 | `[SERIAL][D] f=... sv=... sc=...` | 解析为 `debug` 事件，用于久坐、起夜等姿态判断 |
| `debug on` | 日志终端手动发送 | `[SERIAL] ...` 普通确认，或后续周期性 `[SERIAL][D] ...` | 上位机不强依赖精确 ACK，只显示回包 |
| `debug off` | 日志终端手动发送 | `[SERIAL] ...` 普通确认 | 上位机不强依赖精确 ACK，只显示回包 |

状态回包常用字段：

| 紧凑字段 | 上位机别名 | 说明 |
| --- | --- | --- |
| `f` | `frame` | 帧号 |
| `fs` | `fall_status` | 跌倒状态 |
| `fp` | `fall_prob` | 跌倒概率 |
| `p` | `person_active` | 是否检测到人员 |
| `b` | `base` | 基础检测状态 |
| `fc` | `face` | 人脸检测状态 |
| `hc` | `hand` | 手势检测状态 |
| `gc` | `gesture` | 手势类别 |
| `gs` | `gesture_score` | 手势分数 |
| `om` | `osd_mode` | 当前模式 |
| `pi` | `cfg_print_interval` | 状态打印周期 |
| `fi` | `fps_inst` | 瞬时 FPS |
| `fa` | `fps_avg` | 平均 FPS |
| `dz` | - | 危险区是否启用 |
| `dzn` | - | 危险区顶点数量 |
| `dzh` | - | 人员危险区命中数量 |
| `dza` | - | 人员危险区告警 |
| `pzh` | - | 宠物危险区命中数量 |
| `pza` | - | 宠物危险区告警 |

调试回包常用字段：

| 紧凑字段 | 上位机别名 | 说明 |
| --- | --- | --- |
| `f` | `frame` | 帧号 |
| `sv` | `posture_valid` | 姿态是否有效 |
| `sc` | `posture_class` | 姿态类别，当前按 `0=LYING`、`1=SITTING`、`2=STANDING` 使用 |

## 3. 推理模式命令

| 命令 | 触发入口 | 期望回复 | 上位机判定 |
| --- | --- | --- | --- |
| `base` | 设备控制 > 推理模式 | `[SERIAL] osd_mode set to base` 或 `[SERIAL] inference_mode set to base` | 解析为 `mode_ack`，更新当前模式，并补发一次 `status` |
| `hand` | 设备控制 / 智能家居 | `[SERIAL] osd_mode set to hand` 或 `[SERIAL] inference_mode set to hand` | 同上 |
| `face` | 设备控制 > 推理模式 | `[SERIAL] osd_mode set to face` 或 `[SERIAL] inference_mode set to face` | 同上 |
| `pose` | 设备控制 > 推理模式 | `[SERIAL] osd_mode set to pose` 或 `[SERIAL] inference_mode set to pose` | 同上 |
| `all` | 设备控制 / 姿态控制 / 智能家居 | `[SERIAL] osd_mode set to all` 或 `[SERIAL] inference_mode set to all` | 同上 |

兼容写法：

```text
mode <base|hand|face|pose|all>
```

当前界面按钮发送的是直接模式名，例如 `all`。

## 4. 显示与识别辅助命令

| 命令 | 触发入口 | 期望回复 | 上位机判定 |
| --- | --- | --- | --- |
| `bg on` | 设备控制 > 背景开 | `[SERIAL] ...` 普通确认 | 解析为 `ack` 并显示日志 |
| `bg off` | 设备控制 > 背景关 | `[SERIAL] ...` 普通确认 | 解析为 `ack` 并显示日志 |
| `kp on` | 设备控制 > 关键点开 | `[SERIAL] ...` 普通确认 | 解析为 `ack` 并显示日志 |
| `kp off` | 设备控制 > 关键点关 | `[SERIAL] ...` 普通确认 | 解析为 `ack` 并显示日志 |
| `scan` | 设备控制 > 立即扫脸 | `[SERIAL] ...` 普通确认，或后续人脸相关日志 | 解析为 `ack` 或 `raw` 并显示日志 |

## 5. 人脸录入命令

| 命令 | 触发入口 | 期望回复 | 上位机判定 |
| --- | --- | --- | --- |
| `reg <person_id> <display_name>` | 设备控制 > 人脸录入，采集帧数为默认 `15` 时 | `[SERIAL] enroll request queued: ...`，随后 `[SERIAL] enroll success: ...` | 前者解析为 `enroll_queued`，后者解析为 `enroll_success` |
| `reg <person_id> <display_name> <frames>` | 设备控制 > 人脸录入，采集帧数不是 `15` 时 | 同上 | 同上 |
| `reg cancel` | 设备控制 > 取消录入 | 包含 `enroll request canceled` 或 `no pending enroll` 的文本 | 解析为 `enroll_canceled` |

参数约束：

| 参数 | 约束 |
| --- | --- |
| `person_id` | 非空，不能包含空白字符；整条命令不能超过 24 字符/字节 |
| `display_name` | 非空，不能包含空白字符；建议使用短 ASCII 名称 |
| `frames` | `1` 到 `120`，默认值 `15` 会被上位机省略 |

示例：

```text
reg u1 alice
reg u1 alice 20
reg cancel
```

## 6. 打印周期命令

| 命令 | 触发入口 | 期望回复 | 上位机判定 |
| --- | --- | --- | --- |
| `pi<value>` | 设备控制 > 打印周期 | `[SERIAL] print_interval set to <value>` | 解析为 `interval_ack`，更新 `cfg_print_interval`，并补发一次 `status` |

示例：

```text
pi120
```

说明：上位机使用紧凑别名 `pi120`，不使用较长的 `set print_interval 120`。

## 7. 性能测试命令

| 命令 | 触发入口 | 期望回复 | 上位机判定 |
| --- | --- | --- | --- |
| `test <mode> <sec> <sensor_fps>` | 设备控制 > 性能测试 | `[SERIAL] ...` 普通确认，随后可能输出 `[PERF] ...` | `[SERIAL]` 解析为 `ack`；`[PERF]` 当前作为原始日志显示 |
| `test stop` | 设备控制 > 性能测试 > 停止 | `[SERIAL] ...` 普通确认 | 解析为 `ack` 并显示日志 |

参数约束：

| 参数 | 约束 |
| --- | --- |
| `mode` | `base`、`face`、`pose`、`all`、`each` |
| `sec` | 大于 `0` 的整数 |
| `sensor_fps` | 大于 `0` 的数字 |

示例：

```text
test all 30 15
test each 60 30
test stop
```

## 8. 危险区域命令

危险区坐标使用归一化坐标，范围 `0.0` 到 `1.0`。当前界面按钮不再发送长 `zone set ...`，而是逐条发送短命令并等待确认。

### 8.1 一键下发并启用流程

用户点击一次“下发并启用”后，上位机会自动发送：

```text
zone clear
zone add <x1> <y1>
zone add <x2> <y2>
...
zone add <xn> <yn>
zone on
zone list
```

每一步的期望回复和判定规则：

| 发送命令 | 期望回复 | 状态回包替代确认 | 上位机成功判定 |
| --- | --- | --- | --- |
| `zone clear` | `[SERIAL] danger_zone cleared and disabled.` | `[SERIAL][S] ... dz=0 dzn=0 ...` | 区域关闭且点数为 `0` |
| `zone add <x> <y>` | `[SERIAL] danger_zone add point=(x,y)` | `[SERIAL][S] ... dz=0 dzn=<已添加点数> ...` | 当前追加点已被板端计入 |
| `zone on` | `[SERIAL] danger_zone set to on` | `[SERIAL][S] ... dz=1 dzn=<总点数> ...` | 危险区启用且点数正确 |
| `zone list` | `[SERIAL] danger_zone enabled=on points=<总点数>` | `[SERIAL][S] ... dz=1 dzn=<总点数> ...` | 最终启用状态和点数均正确 |

流程规则：

| 项目 | 行为 |
| --- | --- |
| 发送节奏 | 一次只发送一条，确认后延迟短时间再发下一条 |
| 自动轮询 | 下发期间暂停新的 `status/debug` 自动轮询，避免串口粘包 |
| 单步超时 | 6 秒未收到确认则停止后续命令 |
| 错误处理 | 收到 `[SERIAL][ERROR] ...` 立即停止，并在页面显示失败原因 |
| 坐标精度 | 上位机最多保留 4 位小数并去掉末尾多余的零 |
| 点数限制 | `3` 到 `12` 个点 |

测试矩形按钮发送示例：

```text
zone clear
zone add 0.2 0.2
zone add 0.8 0.2
zone add 0.8 0.8
zone add 0.2 0.8
zone on
zone list
```

### 8.2 危险区手动命令

| 命令 | 触发入口 | 期望回复 | 上位机判定 |
| --- | --- | --- | --- |
| `zone` | 日志终端手动发送 | `[SERIAL] danger_zone ...` 或其他 `[SERIAL] ...` 配置输出 | 显示日志 |
| `zone list` | 姿态控制 > 危险区域 > 查询 | `[SERIAL] danger_zone enabled=<on/off> points=<n>`，或状态回包 `dz/dzn` | 显示日志；一键流程中会核对启用状态和点数 |
| `zone on` | 姿态控制 > 危险区域 > 启用 | `[SERIAL] danger_zone set to on`，或状态回包 `dz=1 dzn=<n>` | 显示日志；一键流程中会核对 |
| `zone off` | 姿态控制 > 危险区域 > 停用 | `[SERIAL] danger_zone set to off`，或状态回包 `dz=0` | 显示日志 |
| `zone clear` | 姿态控制 > 危险区域 > 清空 | `[SERIAL] danger_zone cleared and disabled.`，或状态回包 `dz=0 dzn=0` | 显示日志；一键流程中会核对 |
| `zone add <x> <y>` | 日志终端手动发送；一键流程自动发送 | `[SERIAL] danger_zone add point=(x,y)`，或状态回包 `dzn=<已添加点数>` | 显示日志；一键流程中会核对 |
| `zone pop` | 日志终端手动发送 | `[SERIAL] ...` 普通确认，建议随后 `zone list` 核对 | 显示日志 |
| `zone rect <x1> <y1> <x2> <y2>` | 日志终端手动发送，需满足 24 字符限制 | `[SERIAL] danger_zone rectangle set.`，或状态回包 `dzn=4` | 显示日志 |
| `zone set <x1> <y1> ...` | 日志终端手动发送，需满足 24 字符限制 | `[SERIAL] danger_zone enabled=<on/off> points=<n>` 或普通 `[SERIAL] ...` | 显示日志 |

注意：`zone set` 很容易超过 24 字符，界面按钮不会自动使用它。

## 9. 帮助命令

| 命令 | 触发入口 | 期望回复 | 上位机判定 |
| --- | --- | --- | --- |
| `help` | 日志终端 > 发送 help | `[SERIAL] commands:` 以及命令列表 | 显示日志 |
| `pro` | 日志终端 > 发送 pro | `[SERIAL] ... advanced commands ...` | 显示日志 |

兼容写法：

| 兼容写法 | 推荐写法 |
| --- | --- |
| `h` / `?` | `help` |
| `professional` | `pro` |

## 10. 快捷调参命令

这些命令当前没有专门 UI 控件，但日志终端允许手动发送；仍受 24 字符/字节限制。

| 快捷命令 | 等价完整命令 | 期望回复 | 备注 |
| --- | --- | --- | --- |
| `pa <value>` 或 `pa<value>` | `set pose_ema_alpha <value>` | `[SERIAL] ...` 普通确认或 `[SERIAL][ERROR] ...` | `0~1` |
| `pp <value>` 或 `pp<value>` | `set pose_pending_iou <value>` | `[SERIAL] ...` 普通确认或 `[SERIAL][ERROR] ...` | `0~1` |
| `ps <value>` 或 `ps<value>` | `set pose_switch_frames <value>` | `[SERIAL] ...` 普通确认或 `[SERIAL][ERROR] ...` | `1~120` |
| `ph <value>` 或 `ph<value>` | `set pose_hold_frames <value>` | `[SERIAL] ...` 普通确认或 `[SERIAL][ERROR] ...` | `1~120` |
| `fr <value>` 或 `fr<value>` | `set fall_recover_frames <value>` | `[SERIAL] ...` 普通确认或 `[SERIAL][ERROR] ...` | `0~180` |
| `fd <value>` 或 `fd<value>` | `set fall_decay_unreliable <value>` | `[SERIAL] ...` 普通确认或 `[SERIAL][ERROR] ...` | `0~1` |
| `fi <value>` 或 `fi<value>` | `set face_interval <value>` | `[SERIAL] ...` 普通确认或 `[SERIAL][ERROR] ...` | 正整数 |
| `pi <value>` 或 `pi<value>` | `set print_interval <value>` | `[SERIAL] print_interval set to <value>` | 正整数；界面发送紧凑形式 `pi120` |
| `di <value>` 或 `di<value>` | `set debug_interval <value>` | `[SERIAL] ...` 普通确认或 `[SERIAL][ERROR] ...` | 正整数 |

部分完整 `set ...` 命令名称本身超过 24 字符，会被当前上位机拦截。若板端必须通过此链路调这些参数，应在板端补充不超过 24 字符的短别名。

## 11. 告警与异步回包

这些不是上位机主动命令的直接回复，但会被上位机解析并触发 UI 状态。

| 回包格式 | 上位机事件 | 行为 |
| --- | --- | --- |
| `[SERIAL][PET_DANGER][ALERT] ...` | `pet_danger_alert` | 显示宠物危险区告警 |
| `[SERIAL][PET_DANGER] ... clear ...` | `pet_danger_clear` | 清除宠物危险区告警 |
| `[SERIAL][ERROR] ...` | `error` | 显示错误；危险区事务中会停止后续发送 |
| `[SERIAL][WARN] ...` | `warning` | 显示警告 |
| `[FACE_MATCH] ...` | `raw` | 原始日志显示 |
| `[PERF] ...` | `raw` | 原始日志显示 |
| 其他 `[SERIAL] ...` | `ack` | 普通确认或提示 |
| 其他文本 | `raw` | 原始日志显示 |

## 12. 板端联调检查重点

| 检查项 | 期望 |
| --- | --- |
| 长度 | 板端应按行读取，命令内容至少能完整接收 24 字符，不把 `CRLF` 算入命令内容 |
| 换行 | 每条命令以 `\r\n` 结束，板端不应把下一条命令拼进当前命令 |
| 错误 | 未知命令或参数错误统一返回 `[SERIAL][ERROR] ...`，便于上位机停止事务 |
| 危险区 | 建议板端支持并确认 `zone clear`、`zone add`、`zone on`、`zone list` 四个短命令 |
| 状态字段 | 危险区状态回包中建议稳定输出 `dz` 和 `dzn`，上位机可用它们作为 ACK 替代确认 |
| 手势 | 智能家居依赖 `status` 回包中的 `hc/gc/gs` 或对应长字段，板端不需要额外接收智能家居命令 |
