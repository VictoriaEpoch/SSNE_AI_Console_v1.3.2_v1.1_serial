# 智能家居手势模拟控制说明

## 1. 功能定位

本功能位于上位机的“智能家居”页，用板端 UART 状态中的稳定手势类别和手势分数，模拟控制客厅灯、卧室灯、窗帘、空调和电视。

> 这是上位机内存中的模拟控制，不会向真实家电发送电源、红外、MQTT 或 HTTP 指令。

参考板端协议只定义了 `GA`～`GE` 五个手势类别（紧凑字段 `gc=0`～`gc=4`），没有定义“手掌”、“点赞”等固定语义。因此界面使用 `GA / 类别 0` 这类名称，并允许用户自选动作，避免与实际模型标签混淆。

## 2. 本次增强点

原来的手势控制是一类手势对应一个动作，演示时比较单调。现在保留板端协议不变，在上位机侧扩展出更丰富的交互：

- 每个手势支持“轻触”和“长按”两种触发方式，GA～GE 五类手势可以表达 10 个逻辑指令。
- 动作库扩展到 21 项，覆盖设备开关、灯光亮度、空调温度、电视音量、全部灯光和多个生活场景。
- 新增长按进度显示，手势保持到设定时间后才执行长按动作。
- 新增松手确认逻辑：短按不是一识别就立刻执行，而是在确认手势后等待松手再执行，避免持续手势导致反复开关。
- 新增长按锁定逻辑：长按已经触发后，松手不会再补触发轻触动作。
- 支持短暂掉分缓冲，手势分数瞬间波动不会马上打断识别。
- 设置区增加滚动能力，小窗口下也能完整配置轻触、长按和防误触参数。

## 3. 页面布局

“智能家居”页分为左右两区：

- 左侧是手势设置：启用开关、防误触参数、长按时间、GA～GE 的轻触/长按映射、HAND/ALL 模式快捷按钮，以及长按进度。
- 右侧上方是五个模拟设备卡片，可查看开关、灯光亮度、空调温度和电视音量，也可手动切换。
- 右侧下方是控制记录，显示时间、来源、动作和执行结果。

## 4. 默认映射

| 板端手势 | 状态字段 | 轻触预设 | 长按预设 |
| --- | --- | --- | --- |
| GA / 类别 0 | `gc=0` | 切换客厅灯 | 打开全部灯光 |
| GB / 类别 1 | `gc=1` | 切换卧室灯 | 阅读场景 |
| GC / 类别 2 | `gc=2` | 打开/关闭窗帘 | 观影场景 |
| GD / 类别 3 | `gc=3` | 打开/关闭空调 | 睡眠场景 |
| GE / 类别 4 | `gc=4` | 打开/关闭电视 | 离家场景 |

每个轻触和长按都可独立改为“不执行”、任意设备开关、灯光亮度、空调温度、电视音量或场景动作。点击“应用设置”后会保存映射；“恢复丰富预设”会同时重置轻触、长按和长按时间。

## 5. 动作库

| 类型 | 动作 |
| --- | --- |
| 空动作 | 不执行 |
| 设备开关 | 切换客厅灯、切换卧室灯、打开/关闭窗帘、打开/关闭空调、打开/关闭电视 |
| 灯光调节 | 客厅灯调亮、客厅灯调暗、卧室灯调亮、卧室灯调暗、打开全部灯光、关闭全部灯光 |
| 温度和音量 | 空调调高 1℃、空调调低 1℃、电视音量 +5、电视音量 -5 |
| 场景 | 回家场景、离家场景、观影场景、阅读场景、睡眠场景 |

亮度动作会自动打开对应灯光，范围限制为 `20%`～`100%`；空调温度范围限制为 `16℃`～`30℃`；电视音量范围限制为 `0`～`100`。

## 6. 触发规则

轻触和长按共享同一套防误触识别，但执行时机不同：

- 轻触：手势分数达到阈值并满足连续确认后，松手释放达到设定时间才执行。
- 长按：手势分数达到阈值并满足连续确认后，继续保持到长按时间才执行。
- 长按执行后进入锁定，松手只解除锁定，不会再执行轻触。
- 同一个手势持续保持时只触发一次；需要先松手释放，再做下一次手势。
- 如果某个手势的长按动作设置为“不执行”，该手势会按原来的轻触方式工作。

默认防误触参数：

| 参数 | 默认值 | 作用 |
| --- | --- | --- |
| 手势分数阈值 | `0.50` | 只有 `gs` 达到阈值才认为是有效手势 |
| 连续确认次数 | `1` | 收到同类有效手势后即可确认，板端已经做过手势稳定处理 |
| 松手释放时间 | `0.60` 秒 | 低于阈值持续达到该时间才认为已经松手 |
| 长按触发时间 | `2.5` 秒 | 确认后保持到该时间执行长按 |

## 7. 使用步骤

1. 在“串口连接”页选择设备并连接。
2. 打开“智能家居”页，确保“启用手势模拟家居控制”已勾选。
3. 只演示手势时点击“进入 HAND 模式”；需与人脸、姿态等任务同时运行时点击“进入 ALL 模式”。
4. 按需选择 GA～GE 的轻触和长按动作，设置长按时间（默认 2.5 秒）与防误触参数，再点击“应用设置”。
5. 在摄像头前做出手势：确认后松手会执行轻触动作；继续保持至进度满会执行长按动作。
6. 一次动作后先松手，等待界面提示解除锁定，再做下一次手势。

## 8. 演示建议

推荐先用默认预设演示一遍，能直观看到“同一套 5 个板端手势变成 10 个动作”的效果：

1. 先轻触 GA～GE，分别切换五个设备。
2. 再长按 GA，展示全部灯光打开。
3. 长按 GC，进入观影场景，灯光和窗帘关闭，空调与电视打开。
4. 长按 GD，进入睡眠场景，卧室灯、空调和电视状态会同步变化。
5. 最后长按 GE，执行离家场景，一键关闭全部模拟设备。

如果现场识别不稳定，优先把“连续确认次数”调为 `1`；如果误触偏多，优先把“分数阈值”调高到 `0.80`～`0.85`。

## 9. 防误触逻辑

上位机只使用 `status` 数据驱动家居动作，不使用同期的 `debug` 行重复触发。普通状态轮询约为每秒一次；启用智能家居控制时，上位机会把 `status` 轮询加快到约 250ms 一次，避免短手势被 1 秒刷新间隔漏掉。默认参数如下：

- 手势分数阈值：`0.50`，对应状态字段 `gs`。
- 连续确认次数：`1`，板端已经做过稳定判断，上位机侧以响应速度优先。
- 松手释放时间：`0.60` 秒。
- 长按触发时间：`2.5` 秒，从第一个通过阈值的连续样本开始计时。
- 同一手势执行后会锁定，持续保持手势不会让开关来回翻转。
- 暂时掉分不会立即当作松手，低于阈值持续达到释放时间才确认轻触。
- 重复帧号的状态包会被忽略。

如果演示时触发困难，可适当降低分数阈值或将连续确认次数设为 1；如果有误触，则提高分数阈值或连续确认次数。

## 10. 场景定义

- 回家场景：开启客厅灯、卧室灯、窗帘和空调；电视保持原状态。
- 离家场景：关闭全部五个模拟设备。
- 观影场景：关灯与窗帘，开启 24℃ 空调和电视。
- 阅读场景：客厅灯调至 100%、打开窗帘，关闭其余设备。
- 睡眠场景：卧室灯调至 20%、空调调至 26℃，关闭其余设备。

右侧设备卡片的“手动切换”以及五个场景按钮都不依赖串口，可用于直接演示界面。

## 11. 数据保存与重启

以下内容会随其他上位机设置一起保存：

- 是否启用手势家居控制。
- 分数阈值、连续确认次数、松手释放时间。
- 长按触发时间。
- GA～GE 的轻触与长按动作映射。

模拟设备的当前开关状态和控制记录只保留在本次运行中，重启程序后会回到全关状态。

## 12. 对接真实家电的扩展点

模拟动作集中在 `app/smart_home.py` 的 `SmartHomeController.execute()` 中。如后续要接入真实系统，可保留现有手势确认、映射和界面，再将此处的内存状态更改替换或扩展为 MQTT、HTTP、智能家居网关或独立串口指令。实际对接时建议保留防误触逻辑，并为执行失败、超时和设备离线增加状态回传。
